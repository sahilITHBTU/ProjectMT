import type { ReplaceInFileConfig, ParsedConfig, CliArguments, From } from '../types.ts';
/**
 * Helper to load options from a config file
 */
export declare function loadConfig(file: string): Promise<ReplaceInFileConfig>;
/**
 * Helper to convert a string to Regex if it matches the pattern
 */
export declare function stringToRegex(str: From | From[] | undefined): From | From[] | undefined;
/**
 * Parse config
 */
export declare function parseConfig(config: ReplaceInFileConfig): ParsedConfig;
/**
 * Combine CLI script arguments with config options
 */
export declare function combineConfig(config: ReplaceInFileConfig, argv: CliArguments): ParsedConfig;
