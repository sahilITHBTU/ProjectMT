import type { GlobOptionsWithFileTypesFalse } from 'glob';
import type { ParsedConfig } from '../types.ts';
/**
 * Async wrapper for glob
 */
export declare function globAsync(pattern: string, ignore: string[], allowEmptyPaths: boolean, cfg: GlobOptionsWithFileTypesFalse): Promise<string[]>;
/**
 * Get paths (sync)
 */
export declare function pathsSync(patterns: string[], config: ParsedConfig): string[];
/**
 * Get paths asynchrously
 */
export declare function pathsAsync(patterns: string[], config: ParsedConfig): Promise<string[]>;
